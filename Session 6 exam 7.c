#include<stdio.h>

void main(){
	int a;
	float d;
	char ch, name[20];
	printf("Please Enter the data\n");
	scanf("%d %f %c %s", &a, &d, &ch, name);
	printf("\n The values accpted  are: %d, %f, %c, %s", a,d,ch, name);
	
}
